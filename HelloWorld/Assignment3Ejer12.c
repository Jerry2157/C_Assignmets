/*#include <stdio.h>

int main(void) {

	int rep;
	scanf("%i", &rep);
	char phrase[25];

	scanf("%s", phrase, sizeof(phrase));

	printStrings(phrase,rep);

	return 0;
}
int printStrings(char phrase, int rep) {
	for (int i = 0; i < rep; i++) {
		printf("%s", phrase);
	}
	return 0;
}*/