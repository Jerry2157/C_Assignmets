/*#include <stdio.h>
#include <math.h>

int main(void) {

	//int base;
	//scanf("%d", &base);
	int num;
	scanf("%d", &num);

	printNumbers(num);
	return 0;
}

int printNumbers(num) {

	for (int i = 1; i <= num; i++) {
		printf("[%d", i);
		if (i < num) {

			printf("] ");
		}
		else {
			printf("]");
		}
	}

	return 0;
}*/