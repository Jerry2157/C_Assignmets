/*#include <stdio.h>

int main(void) {
	int fila;
	scanf("%d", &fila);
	int cols;
	scanf("%d", &cols);

	int sumador = 0;

	for (int i = 1; i <= fila; i++) {
		sumador = i;
		for (int j = 0; j < cols; j++) {
			printf("%d", sumador);
			if (j < (cols - 1)) {
				printf(", ");
			}
			sumador += fila;
		}
		printf("\n");
		
	}
	return 0;
}*/