/*#include <stdio.h>

int main(void) {

	double tempf = 98.6;
	double tempc = 0.0;
	double result = ftoc(tempf, tempc);
	printf("Body temp in C is: %lf",result);

	return 0;
}

double ftoc(double tempf, double tempc) {
	tempc = (tempf - 32) * 5 / 9;
	return tempc;
}*/