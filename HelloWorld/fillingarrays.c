/*#include <stdio.h>
#define SIZE 100000

int main(void) {
	unsigned int arraySize;
	scanf("%u", &arraySize);

	int a[SIZE];
	int i;
	for (i = 0; i < arraySize; i++) {
		scanf("%u", &a[i]);
		puts("These are the elements of the array: ");
		for (i = 0; i < arraySize; i++) {
			printf("%u", a[i]);
		}
		return 0;
	}
}*/