/*#include <stdio.h>
#define SIZE 10000

int main(void) {
	unsigned int h[SIZE];
	unsigned n = 0; 
	scanf("%u", &n);
	for (unsigned i = 0; i < n; i++) {
		scanf("%u", &h[i]);
	}
	unsigned max = h[0];
	for (int i = 1; i < n; i++) {
		if (h[i] > max) {
			max = h[i];
		}
		else {

		}
	}
	printf("%u", max);
	return 1;
}*/